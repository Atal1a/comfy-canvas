# Contributing

欢迎提交问题和改进。界面问题请附截图、浏览器版本和复现步骤。

基本要求：

- 不提交模型、输出、数据库、证书、密钥、账号数据或外部 checkout；
- 新增工作流必须提供中立默认值、依赖清单、授权来源和有针对性的测试；
- 前端改动复用现有组件与设计变量；
- 提交前运行 `npm run audit:content`、`npm run build` 和移动端单元测试。

Pull Request 应说明用户可见影响、依赖变化、实际测试命令和已知限制。工作流、模型清单或运行时边界变化必须单独列出。
